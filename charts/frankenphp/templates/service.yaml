apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-frankenphp.fullname" . }}
  labels:
    {{- include "helm-frankenphp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-frankenphp.selectorLabels" . | nindent 4 }}
